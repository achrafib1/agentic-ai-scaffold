"""SQLAlchemy persistence models.

Define declarative mappings in this package and import them here when model
registration is needed by migration or metadata tooling. Domain types that do
not depend on SQLAlchemy belong in ``shared.domain`` instead.
"""
